export enum CheckFrequency {
  OnLoad = 'On Load',
  Daily = 'Daily',
}

export enum UpgradeChannel {
  Latest = 'Latest',
  Next = 'Next',
}

export enum UpdateArgs {
  next = '--next',
  force = '--force',
}
